const WishlistResults = ({ title, wishlist }) => {
  
    return (
      <>
        <p>{title}</p>
        <br />
        <br />
      </>
    );
  };
  
  export default WishlistResults;

// sets - set_num, name, num parts, set_img_url, set_url, year, theme_id (last 2 uncommon)
// minifigs - set_num, name, num_parts, set_img_url, set_url
